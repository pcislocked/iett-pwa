All frontend UI/UX and accessibility issues assigned to the `iett-pwa` repository have been fully resolved. The changes have been pushed and the test suite passes successfully.

Here is the checklist of what has been fixed in this PR:

### Frontend (iett-pwa)
- [x] **API Client Violation:** Replaced native `fetch()` calls in `BusDetailSheet.tsx` with proper typed endpoints (`api.fleet.detail`) to keep API consumption standard.
- [x] **CSS Class Bug:** Refactored `${!showRouteButton && 'bg-surface'}` inside `RoutePage.tsx` using a clean Ternary operator (`showRouteButton ? '' : 'bg-surface'`) to prevent returning `false` as a class string.
- [x] **Unused Variables / Build Errors:** Passed the `const center = { lat, lng }` into the `<MapContainer center={center}>` component in `RoutePage.tsx` to fix unused variables blocking the build.
- [x] **Import Scope Errors:** Moved the `BusDetailSheet` import to the top of `RoutePage.tsx` (module scope) instead of having it inside the component body, which broke the bundler.
- [x] **Accessibility (a11y):** Changed the clickable triggering `<div>` inside `VariantSelect.tsx` to an actual `<button type="button">`, adding proper `aria-haspopup` and `aria-expanded` attributes for screen readers and keyboard navigation.
- [x] **Scrolling Glitches:** Removed the disruptive `window.scrollTo(0, 0)` call in `RoutePage.tsx` which caused unexpected scrolling issues upon route loading.
- [x] **Null Routing Guard:** Guarded against `routeCode` being `null` before rendering `<Link>` tags in `BusDetailSheet.tsx` ensuring the app doesn't crash or generate bad navigation paths.
- [x] **TypeScript Warnings:** Added missing type definitions in `BusDetailSheet.tsx` and `RoutePage.tsx` to fix strict ESLint checks blocking the test coverage suite.

All tests, TS compiler (`typecheck`), and linters run clean. Coverage is maintained at ~75%.
